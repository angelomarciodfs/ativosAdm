// AI Service disabled per user request.
// This file is kept to avoid "file not found" errors if referenced elsewhere, 
// but it contains no active code or imports that would break the build.

export const generateAIResponse = async () => {
  console.warn("AI Service is disabled.");
  return null;
};